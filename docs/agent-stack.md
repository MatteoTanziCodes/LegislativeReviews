For the search engine:

Intent Agent — classifies user request

Jurisdiction Agent — federal/provincial/municipal routing later

Domain Router Agent — maps to legal area

Retrieval Planner Agent — expands query, chooses filters

Authority Reader Agent — reads top retrieved sections

Comparison Agent — optional for compare mode

Answer Composer Agent — outputs final citation-backed answer